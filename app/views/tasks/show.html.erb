
  <h2><%= @task.title %></h2>
  <p><%= @task.details %></p>
  <p>Completed? <%= @task.completed %></p>

<%= link_to 'edit',  edit_task_path(@task) %>
<%= link_to 'delete',  task_path(@task), method: 'delete', data: { confirm: "Are you sure?" }%> <br><br>
<%= link_to 'back to Tasks',  tasks_path %>
